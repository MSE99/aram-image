
name: Run tests CI

jobs:
  run-tests:
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with:
          node-version: 18
      - run: npm run encode
      - run: npm run decode
      - run: npm test
